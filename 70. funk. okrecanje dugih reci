function spinWords(string) {
    arr = string.split(" ")
    for (i = 0; i < arr.length; i++) {
        if (arr[i].length >= 5) {
            arr[i] = arr[i].split("").reverse().join("")
        }
    }
    return arr.join(" ")
}
console.log(spinWords("Java Script je bas zanimljiv"))
