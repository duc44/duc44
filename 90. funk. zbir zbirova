function digitalRoot(n) {
    for (temp = 10; temp >= 10;) {
        temp = 0
        for (i = 0; i < (n + "").split("").length; i++) {
            temp += Number((n + "").split("")[i])
        }
        n = temp
    }
    return n
}
console.log(digitalRoot(132189))
